# PortfolioPilot - data ingestion / quant / ML pipeline dependencies
# Trimmed from a full `pip freeze` to only what fetch_*.py, compute_ratios.py,
# price_signals.py, feature_engineering.py, train_classifiers.py,
# lasso_selection.py, cluster_companies.py, build_vector_store.py, retrieve.py,
# agent.py, and agent_gemini.py actually import. Versions pinned to match
# what's confirmed working on the local dev machine.

# --- Data ingestion ---
pandas==3.0.3
numpy==2.4.6
pyarrow==25.0.1
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.2
yfinance==1.6.0

# --- Quant & ML (Layer 2) ---
scikit-learn==1.9.1
xgboost==3.4.1

# --- Retrieval / NLP (Layer 3) ---
sentence-transformers==6.0.1

# --- Agent orchestration (Layer 4) ---
anthropic==1.5.0
google-genai==2.23.0
